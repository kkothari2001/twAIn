class History{
  String preCommand; // Precommand is the green part
  String command;  //Command is what the user enters
  int type; //Type is what type of display it features

  History({this.preCommand, this.command, this.type});
}